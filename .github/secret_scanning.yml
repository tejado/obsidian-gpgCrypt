# The committed OpenPGP keys under these paths are TEST-ONLY fixtures generated by
# scripts/gen-test-fixtures.mjs (user ids "gpgCrypt TEST-ONLY …"). They protect nothing.
paths-ignore:
  - "tests/fixtures/keys/**"
  - "e2e/vaults/**/*.asc"
